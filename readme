functions/templates/etc for unit testing bash

in a bash script
  where everything is contained in a function
    and the main function is prefixed as described

```
test $SOURCE_FOR_UNIT_TESTING || main "$@"
```

you can use the test template to source and test functions in your script

license: MIT
